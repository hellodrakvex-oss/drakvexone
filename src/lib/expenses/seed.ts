// Seed data has been removed.
// All expense records are now fetched from the Supabase expenses table.
// This file is kept as a stub to prevent import errors during the migration period.

import type { Expense } from "./types";

/**
 * @deprecated Seed data removed. All expenses come from Supabase.
 * Returns empty array — no demo/mock data.
 */
export function generateSampleExpenses(): Expense[] {
  return [];
}
