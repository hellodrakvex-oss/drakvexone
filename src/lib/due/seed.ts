// Seed data has been removed.
// All due records are now fetched from the Supabase customer_dues table.
// This file is kept as a stub to prevent import errors during the migration period.

import type { CustomerDue } from "./types";

/**
 * @deprecated Seed data removed. All dues come from Supabase.
 * Returns empty array — no demo/mock data.
 */
export function createSeedDues(): CustomerDue[] {
  return [];
}
